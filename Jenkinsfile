pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Nermin - Way to Reinvent Yourself'
                sh 'python --version'
                sh 'python pipeline.py'
            }
        }
    }
}